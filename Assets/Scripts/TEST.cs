using UnityEngine;

public class TEST : MonoBehaviour{
	public GameMap map;

	void Start(){
		/*
		for(int i=0; i<this.map.GetSize()*this.map.GetSize(); i++){
			this.map.SetQuad(i, new Quad(i%3));
			this.map.BuildQuad(i);
		}
		*/
	}
}